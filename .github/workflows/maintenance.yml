name: Maintenance

on:
  schedule:
    # Weekly: Sunday 06:00 UTC
    - cron: '0 6 * * 0'
  workflow_dispatch: # Allow manual trigger

jobs:
  maintain:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run maintenance pipeline
        id: pipeline
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: node scripts/maintenance/maintain.cjs --verbose

      # Capture current-run outputs before create-pull-request restores the base checkout.
      - name: Build summary
        id: summary
        if: always()
        run: node scripts/maintenance/summary.cjs

      - name: Preserve maintenance report
        if: always() && steps.summary.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: maintenance-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: data/maintenance-report.json
          if-no-files-found: error
          retention-days: 14

      - name: Preserve generated maintenance outputs
        if: always() && (steps.pipeline.outcome == 'success' || steps.pipeline.outcome == 'failure')
        uses: actions/upload-artifact@v4
        with:
          name: maintenance-outputs-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            data/exercises-generated.json
            data/exercise-videos.json
            data/federation-discoveries.json
            data/teams-lv.json
            data/teams-ee.json
            data/teams-lt.json
            data/content-queue.json
            data/team-health-report.json
            src/data/articles.ts
            src/data/quizzes.ts
            src/data/quotes.ts
            src/data/exercises.ts
            src/i18n/en.json
            src/i18n/ru.json
            src/i18n/lv.json
            src/i18n/es.json
            src/i18n/et.json
            src/i18n/lt.json
          if-no-files-found: error
          retention-days: 14

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Create PR with discoveries
        id: pr
        if: success() && steps.pipeline.outcome == 'success' && steps.changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          # NOT GITHUB_TOKEN. "Allow GitHub Actions to create and approve pull
          # requests" is off on every repo in the fleet but two, so the default
          # token fails this step with "GitHub Actions is not permitted to create
          # or approve pull requests" — which is what killed this workflow on its
          # very first scheduled run. Enabling that setting would also grant
          # Actions the right to *approve* PRs, which would hollow out the review
          # gate everywhere; a PAT scoped to this repo buys only what is needed.
          # It also means the maintenance PR triggers CI, which a GITHUB_TOKEN PR
          # deliberately does not.
          token: ${{ secrets.GH_PAT }}
          commit-message: 'chore: maintenance run — team discoveries + content updates'
          title: '⚽ Weekly maintenance: team discoveries & data updates'
          body: |
            Automated maintenance run. Changes may include:
            - New team discoveries from federation scanners
            - Exercise video enrichments
            - Content queue status updates
            - Maintenance report

            Review `data/federation-discoveries.json` for new teams.
            Set `status: "approved"` on entries you want to merge into the registry.
          branch: maintenance/weekly-run
          delete-branch: true
          labels: maintenance,automated

      - name: Notify Telegram (success)
        if: success()
        env:
          NAURO_BOT_TOKEN: ${{ secrets.NAURO_BOT_TOKEN }}
          NAURO_CHAT_ID: ${{ secrets.NAURO_CHAT_ID }}
          MAINTENANCE_SUMMARY: ${{ steps.summary.outputs.message }}
        run: |
          bash scripts/notify-telegram.sh --plain "$MAINTENANCE_SUMMARY"

      - name: Notify Telegram (failure)
        if: failure()
        env:
          NAURO_BOT_TOKEN: ${{ secrets.NAURO_BOT_TOKEN }}
          NAURO_CHAT_ID: ${{ secrets.NAURO_CHAT_ID }}
          MAINTENANCE_SUMMARY: ${{ steps.summary.outputs.message || 'No current-run report available; inspect the failed step.' }}
          # Which step actually failed. "maintenance FAILED" plus a link means
          # opening a laptop and reading a 500-line log to learn anything at all;
          # the first scheduled run of this workflow sent exactly that and the
          # cause (a permissions setting, not the pipeline) stayed invisible.
          FAILED_STEP: >-
            ${{ steps.pipeline.outcome == 'failure' && 'maintenance task errors — see the report artifact'
             || steps.summary.outcome == 'failure' && 'current-run report validation'
             || steps.changes.outcome == 'failure' && 'change detection'
             || steps.pr.outcome == 'failure' && 'PR creation — check GH_PAT has Pull requests: write'
             || 'the maintenance pipeline (scripts/maintenance/maintain.cjs)' }}
        run: |
          bash scripts/notify-telegram.sh --plain "❌ golazo maintenance FAILED
          Failed at: ${FAILED_STEP}
          ${MAINTENANCE_SUMMARY}
          Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
